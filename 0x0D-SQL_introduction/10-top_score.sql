-- A script that lists all records of the tale second_table of the database hbtn_0c_0
-- Results should display both the score and the name
-- Records should be ordered (top first).
SELECT score, name FROM second_table ORDER BY score DESC; 
